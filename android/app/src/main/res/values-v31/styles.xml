<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Android 12+ replaces the classic launch drawable with a system
         splash (app icon on a plain background). We brand it: white
         background + the full "Let's PADEL" logo, so the very first
         screen already shows the club name. -->
    <style name="LaunchTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <item name="android:windowSplashScreenBackground">#FFFFFF</item>
        <item name="android:windowSplashScreenBrandingImage">@drawable/splash_branding</item>
        <item name="android:windowBackground">@drawable/launch_background</item>
    </style>
    <style name="NormalTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <item name="android:windowBackground">?android:colorBackground</item>
    </style>
</resources>
