import 'package:firebase_core/firebase_core.dart';

/// PLACEHOLDER — replaced automatically when you connect your own
/// Firebase project by running:
///
///   flutterfire configure
///
/// (Full instructions in SETUP.md, step 2.) Until then the app starts in
/// "setup required" mode and shows a friendly explanation screen.
class DefaultFirebaseOptions {
  static FirebaseOptions get currentPlatform {
    throw UnsupportedError(
      'Firebase is not configured yet. Run `flutterfire configure` — see SETUP.md.',
    );
  }
}
